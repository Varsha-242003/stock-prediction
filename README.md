# stock-prediction
Real-time stock price prediction.
This project is a built using HTML, CSS and JAVASCRIPT, that can update the price of each subscribed stocks asynchronously, without reloading page.
This also supports multiple logins (min 2 users) synchronously and does not effect the operations of other users or the system.
